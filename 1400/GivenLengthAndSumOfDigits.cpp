//#227 C
//1400

#include <bits/stdc++.h>

#define ll long long
#define ld long double
#define ar array

#define pb push_back
#define vt vector
#define REP(i,a,b) for(int i = a; i < b; i++)


using namespace std;

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	
	int m,s;
	cin >> m>> s;
	
	ll min, max;
	if(m == 0 || s == 0){
		min = -1;
		max = -1;
	}
	
	
	
	cout << min << " " << max << endl;
}
